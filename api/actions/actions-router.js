// Write your "actions" router here!
const express = require('express');
const Action = require('./actions-model');
const am = require('./actions-middleware');

const router = express.Router();

module.exports = router;